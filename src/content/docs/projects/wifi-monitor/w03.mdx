---
title: "3차시: 차트와 시각화"
description: "RSSI 이력 그래프 + AP 채널 분포 + 게이지 — 데이터를 그림으로 보여주는 대시보드"
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

{/* 프로젝트 진행 바 */}
<div class="project-progress">
  <div class="prog-item prog-1 done">1. 첫 웹서버</div>
  <div class="prog-item prog-2 done">2. 실시간 대시보드</div>
  <div class="prog-item prog-3 current">3. 차트 시각화</div>
  <div class="prog-item prog-4">4. 신호 지도</div>
  <div class="prog-item prog-5">5. 나만의 기능</div>
  <div class="prog-item prog-6">6. 발표</div>
</div>

{/* 결과물 미리보기 */}
<div class="outcome-preview">
  <div class="outcome-badge">이 차시가 끝나면</div>
  <h4>데이터가 그래프로 살아납니다</h4>
  <p>숫자만 나열된 대시보드가 <strong>꺾은선 그래프, 반원 게이지, 채널 분포 막대차트</strong>로 진화합니다. 서버(Python)는 그대로 두고, 브라우저(JavaScript)가 Canvas에 그래프를 그리는 <strong>클라이언트 사이드 시각화</strong>를 체험합니다.</p>
  <div class="outcome-items">
    <div class="outcome-item">
      <div class="item-icon">📈</div>
      <div class="item-label">꺾은선 그래프</div>
      RSSI 이력 30개 포인트
    </div>
    <div class="outcome-item">
      <div class="item-icon">🎯</div>
      <div class="item-label">반원 게이지</div>
      현재 신호 세기 한눈에
    </div>
    <div class="outcome-item">
      <div class="item-icon">📊</div>
      <div class="item-label">채널 분포</div>
      AP 채널별 막대차트
    </div>
    <div class="outcome-item">
      <div class="item-icon">🤖</div>
      <div class="item-label">바이브 코딩</div>
      나만의 차트 디자인
    </div>
  </div>
</div>

> **오늘의 질문:** 숫자 나열 vs 그래프 — 어느 쪽이 더 빠르게 이해될까?

<div class="lesson-overview">
  <h4>수업 개요</h4>
  <div class="meta-grid">
    <div class="meta-item">
      <div class="meta-label">소요시간</div>
      50분
    </div>
    <div class="meta-item">
      <div class="meta-label">형태</div>
      개인 실습
    </div>
    <div class="meta-item">
      <div class="meta-label">난이도</div>
      ●●●●○
    </div>
    <div class="meta-item">
      <div class="meta-label">선수학습</div>
      2차시 — 실시간 데이터 대시보드
    </div>
  </div>
</div>

## 준비물

<div class="prep-checklist">
  <h5>하드웨어</h5>
  <ul>
    <li>Raspberry Pi Pico 2 WH</li>
    <li>Micro USB 데이터 케이블</li>
  </ul>
  <h5>소프트웨어</h5>
  <ul>
    <li>Thonny IDE</li>
    <li>AI 접근 환경 — Claude, ChatGPT 등</li>
    <li>스마트폰 (대시보드 확인용)</li>
  </ul>
</div>

---

## 숫자 vs 그래프

같은 RSSI 데이터인데, 표현 방식만 다릅니다.

<div class="compare-cards">
  <div class="compare-card">
    <strong>숫자 나열</strong><br/>
    -52, -55, -48, -61, -53
    <div style="color: #888; margin-top: 8px;">추세 파악이 어려움</div>
  </div>
  <div class="compare-card">
    <strong>꺾은선 그래프</strong><br/>
    한눈에 추세가 보임
    <div style="color: #888; margin-top: 8px;">변화의 방향과 크기를 즉시 파악</div>
  </div>
</div>

그래프는 **추세**를, 게이지는 **현재 상태**를, 막대차트는 **비교**를 보여줍니다. 오늘 이 세 가지를 모두 대시보드에 넣어봅니다.

---

## 3차시에 추가되는 것들

2차시 코드에서 **시각화 3종**이 추가됩니다.

<div class="code-map">
  <div class="map-row">
    <div class="map-num n1">①</div>
    <div class="map-content">
      <div class="map-title">반원 게이지</div>
      <div class="map-desc">CSS transform으로 RSSI를 반원 바늘로 표시</div>
    </div>
    <div class="map-note">현재 상태를 직관적으로</div>
  </div>
  <div class="map-row">
    <div class="map-num n2">②</div>
    <div class="map-content">
      <div class="map-title">RSSI 이력 꺾은선 <span class="map-tag">Canvas API</span></div>
      <div class="map-desc">최근 30개 데이터 포인트를 실시간 그래프로</div>
    </div>
  </div>
  <div class="map-row">
    <div class="map-num n3">③</div>
    <div class="map-content">
      <div class="map-title">채널 분포 막대차트</div>
      <div class="map-desc">AP 스캔 결과를 채널 1~13 기준 막대차트로</div>
    </div>
  </div>
</div>

---

## 코드 실행

<Aside type="tip" title="복사 & 붙여넣기">
코드 블록 오른쪽 위의 복사 버튼을 클릭하면 전체 코드가 복사됩니다. Thonny에 붙여넣고 Pico에 저장하세요. `wifi_config.py`는 그대로 사용합니다.
</Aside>

<div class="code-file-label">main.py — Pico에 저장 (2차시 코드 덮어쓰기)</div>

```python collapse={10-255}
# ============================================
# 교실 Wi-Fi 모니터 - 3차시
# 차트 시각화 (게이지 + 꺾은선 + 채널 분포)
# ============================================
import network
import socket
import time
import gc
from wifi_config import WIFI_SSID, WIFI_PASSWORD

# ── Wi-Fi 연결 ──────────────────────────────
wlan = network.WLAN(network.STA_IF)
wlan.active(True)
print("Wi-Fi 연결 중...")
wlan.connect(WIFI_SSID, WIFI_PASSWORD)
wait = 0
while not wlan.isconnected():
    time.sleep(1)
    wait += 1
    if wait > 15:
        print("연결 실패!")
        raise SystemExit
my_ip = wlan.ifconfig()[0]
print(f"연결 완료! IP: {my_ip}")

visit_count = 0
start_time = time.ticks_ms()

# ── RSSI 이력 (최근 30개) ───────────────────
rssi_history = []
MAX_HISTORY = 30

# ── AP 스캔 (캐시) ──────────────────────────
ap_cache = []
last_scan = 0
SCAN_INTERVAL = 30

def scan_ap():
    try:
        results = wlan.scan()
        aps = []
        for ap in sorted(results, key=lambda x: x[3], reverse=True)[:8]:
            ssid = ap[0].decode('utf-8', 'ignore')
            if not ssid:
                continue
            aps.append((ssid, ap[3], ap[2], ap[4]))
        return aps
    except:
        return []

def get_ap_cached():
    global ap_cache, last_scan
    now = time.ticks_ms()
    if time.ticks_diff(now, last_scan) > SCAN_INTERVAL * 1000:
        ap_cache = scan_ap()
        last_scan = now
    return ap_cache

# ── 정적 CSS (f-string 아님 → 중괄호 그대로) ──
PAGE_CSS = """<style>
*{margin:0;padding:0;box-sizing:border-box}
body{font-family:-apple-system,sans-serif;background:#0f0f23;color:#ccc;padding:12px}
.hdr{text-align:center;padding:8px 0 12px}
.hdr h1{color:#00d4ff;font-size:1.3em}
.hdr .sub{color:#555;font-size:0.8em;margin-top:4px}
.grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;max-width:500px;margin:0 auto}
.card{background:#1a1a3e;border-radius:12px;padding:12px;text-align:center;border:1px solid #2a2a5e}
.card.wide{grid-column:span 2}
.lbl{color:#888;font-size:0.75em;margin-bottom:4px}
.val{color:#00d4ff;font-size:1.2em;font-weight:bold}
.gauge-wrap{position:relative;width:160px;height:90px;margin:8px auto}
.gauge-bg{width:160px;height:80px;border-radius:80px 80px 0 0;background:conic-gradient(from 0.75turn,#ff6b6b,#ffd43b,#51cf66);overflow:hidden}
.gauge-needle{position:absolute;bottom:0;left:50%;width:3px;height:70px;background:#fff;transform-origin:bottom center;margin-left:-1.5px;border-radius:2px;transition:transform 0.5s}
.gauge-center{position:absolute;bottom:-4px;left:50%;width:12px;height:12px;background:#fff;border-radius:50%;margin-left:-6px}
canvas{width:100%;height:140px;display:block;margin-top:8px}
.ap-tbl{width:100%;border-collapse:collapse;font-size:0.8em;margin-top:8px}
.ap-tbl th{color:#888;font-weight:normal;text-align:left;padding:4px;border-bottom:1px solid #2a2a5e}
.ap-tbl td{padding:4px;border-bottom:1px solid #1a1a3e}
.ft{text-align:center;margin-top:12px;color:#333;font-size:0.7em}
</style>"""

# ── 차트 JavaScript (f-string 아님) ─────────
CHART_JS = """<script>
function drawChart(id,data){
var c=document.getElementById(id);if(!c)return;
var ctx=c.getContext('2d'),W=c.width,H=c.height;
ctx.clearRect(0,0,W,H);var n=data.length;if(n<2)return;
var mn=Math.min.apply(null,data),mx=Math.max.apply(null,data);
if(mn==mx){mn-=5;mx+=5}
var p=25;
ctx.strokeStyle='#2a2a5e';ctx.lineWidth=1;
for(var i=0;i<5;i++){var y=p+(H-2*p)*i/4;
ctx.beginPath();ctx.moveTo(p,y);ctx.lineTo(W-5,y);ctx.stroke();
ctx.fillStyle='#555';ctx.font='10px sans-serif';ctx.textAlign='right';
ctx.fillText((mx-(mx-mn)*i/4).toFixed(0),p-3,y+3);}
ctx.beginPath();ctx.strokeStyle='#00d4ff';ctx.lineWidth=2;
for(var i=0;i<n;i++){var x=p+(W-p-5)*i/(n-1);
var y=p+(H-2*p)*(mx-data[i])/(mx-mn);
if(i==0)ctx.moveTo(x,y);else ctx.lineTo(x,y);}ctx.stroke();
var avg=0;for(var i=0;i<n;i++)avg+=data[i];avg/=n;
var ay=p+(H-2*p)*(mx-avg)/(mx-mn);
ctx.setLineDash([4,4]);ctx.strokeStyle='#ffd43b';ctx.lineWidth=1;
ctx.beginPath();ctx.moveTo(p,ay);ctx.lineTo(W-5,ay);ctx.stroke();
ctx.setLineDash([]);ctx.fillStyle='#ffd43b';ctx.font='10px sans-serif';
ctx.textAlign='left';ctx.fillText('avg '+avg.toFixed(0),W-50,ay-4);}

function drawChannelChart(id,ch){
var c=document.getElementById(id);if(!c)return;
var ctx=c.getContext('2d'),W=c.width,H=c.height;
ctx.clearRect(0,0,W,H);
var mx=Math.max.apply(null,ch);if(mx==0)mx=1;
var bw=(W-40)/13;
for(var i=0;i<13;i++){var x=25+i*bw;
var h=(H-30)*ch[i]/mx;
var hue=ch[i]>0?(120-120*ch[i]/mx):120;
ctx.fillStyle='hsl('+hue+',80%,55%)';
ctx.fillRect(x,H-20-h,bw-3,h);
ctx.fillStyle='#888';ctx.font='9px sans-serif';ctx.textAlign='center';
ctx.fillText(''+(i+1),x+bw/2-1,H-6);
if(ch[i]>0){ctx.fillStyle='#ccc';ctx.fillText(ch[i],x+bw/2-1,H-24-h);}}
}
window.onload=function(){
drawChart('lineChart',RSSI_DATA);
drawChannelChart('barChart',CH_DATA);}
</script>"""

# ── HTML 페이지 ─────────────────────────────
def make_page():
    gc.collect()
    rssi = wlan.status('rssi')
    rssi_history.append(rssi)
    if len(rssi_history) > MAX_HISTORY:
        rssi_history.pop(0)

    uptime_sec = time.ticks_diff(time.ticks_ms(), start_time) // 1000
    mem_free = gc.mem_free()
    mem_pct = round(gc.mem_alloc() / (mem_free + gc.mem_alloc()) * 100, 1)
    rssi_pct = max(0, min(100, (rssi + 90) * 100 // 60))

    if rssi > -50:
        quality, qcolor = "최고", "#51cf66"
    elif rssi > -60:
        quality, qcolor = "좋음", "#94d82d"
    elif rssi > -70:
        quality, qcolor = "보통", "#ffd43b"
    elif rssi > -80:
        quality, qcolor = "약함", "#ff922b"
    else:
        quality, qcolor = "매우 약함", "#ff6b6b"

    if uptime_sec < 60:
        uptime = f"{uptime_sec}초"
    elif uptime_sec < 3600:
        uptime = f"{uptime_sec // 60}분 {uptime_sec % 60}초"
    else:
        uptime = f"{uptime_sec // 3600}시간 {(uptime_sec % 3600) // 60}분"

    needle_deg = -90 + rssi_pct * 180 // 100
    rssi_str = ",".join(str(v) for v in rssi_history)

    aps = get_ap_cached()
    channels = [0] * 13
    for ap in aps:
        ch = ap[2]
        if 1 <= ch <= 13:
            channels[ch - 1] += 1
    ch_str = ",".join(str(v) for v in channels)

    ap_rows = ""
    for ap in aps:
        ssid, sig, ch, auth = ap
        sec = ["OPEN", "WEP", "WPA", "WPA2", "WPA/2"][min(auth, 4)]
        mark = "* " if ssid == WIFI_SSID else ""
        tc = "#00d4ff" if ssid == WIFI_SSID else "#ccc"
        pct = max(0, min(100, (sig + 90) * 100 // 60))
        if sig > -50: sc = "#51cf66"
        elif sig > -70: sc = "#ffd43b"
        else: sc = "#ff6b6b"
        ap_rows += f'<tr><td style="color:{tc}">{mark}{ssid}</td><td><span style="display:inline-block;height:6px;width:{pct}%;max-width:60px;background:{sc};border-radius:3px;"></span> {sig}</td><td>{ch}</td><td>{sec}</td></tr>'
    if not ap_rows:
        ap_rows = '<tr><td colspan="4" style="color:#555;">스캔 중...</td></tr>'

    return f"""<!DOCTYPE html>
<html><head><meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<meta http-equiv="refresh" content="5">
<title>Wi-Fi 시각화</title>
{PAGE_CSS}
</head><body>
<div class="hdr"><h1>Wi-Fi 시각화 대시보드</h1>
<div class="sub">실시간 모니터링 · 5초 자동 갱신 · 데이터 {len(rssi_history)}개</div></div>
<div class="grid">
<div class="card wide">
<div class="lbl">Wi-Fi 신호 세기</div>
<div class="gauge-wrap">
<div class="gauge-bg"></div>
<div class="gauge-needle" style="transform:rotate({needle_deg}deg);background:{qcolor};"></div>
<div class="gauge-center"></div>
</div>
<div class="val">{rssi} <span style="font-size:0.6em">dBm</span>
<span style="font-size:0.5em;margin-left:8px;color:{qcolor}">{quality}</span></div>
</div>
<div class="card wide">
<div class="lbl">RSSI 이력 (최근 {len(rssi_history)}개)</div>
<canvas id="lineChart" width="460" height="140"></canvas>
</div>
<div class="card wide">
<div class="lbl">채널 분포 (AP 수)</div>
<canvas id="barChart" width="460" height="140"></canvas>
</div>
<div class="card">
<div class="lbl">가동 시간</div>
<div class="val">{uptime}</div></div>
<div class="card">
<div class="lbl">메모리</div>
<div class="val">{mem_pct}%</div></div>
<div class="card wide">
<div class="lbl">주변 Wi-Fi 네트워크</div>
<table class="ap-tbl"><thead><tr><th>이름</th><th>신호</th><th>채널</th><th>보안</th></tr></thead>
<tbody>{ap_rows}</tbody></table>
</div></div>
<div class="ft">Powered by MicroPython on Pico 2 WH</div>
<script>var RSSI_DATA=[{rssi_str}];var CH_DATA=[{ch_str}];</script>
{CHART_JS}
</body></html>"""

# ── 웹서버 ───────────────────────────────────
server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
server.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
server.bind(('0.0.0.0', 80))
server.listen(2)
server.settimeout(1.0)  # accept()를 1초만 기다림 → 유휴 시간에 스캔
print()
print("=" * 45)
print(f"  시각화 대시보드 시작! http://{my_ip}")
print("=" * 45)

while True:
    try:
        client, addr = server.accept()
        request = client.recv(1024).decode()
        if 'favicon' in request:
            client.close()
            continue
        visit_count += 1
        print(f"접속 #{visit_count}: {addr[0]}")
        page = make_page()
        client.send(b"HTTP/1.1 200 OK\r\nContent-Type: text/html; charset=utf-8\r\nConnection: close\r\n\r\n")
        client.send(page.encode())
        client.close()
    except OSError:
        # 1초 동안 접속 없음 → 이 틈에 AP 스캔 (응답 지연 방지)
        get_ap_cached()
    except Exception as e:
        print(f"에러: {e}")
        try:
            client.close()
        except:
            pass
```

<Aside type="note" title="2차시 대비 변경점">
2차시의 서버사이드 구조를 유지하되, **게이지는 CSS transform**, **꺾은선/막대차트는 JavaScript Canvas**로 그립니다. 서버(Python)가 데이터를 수집하고 JavaScript 데이터와 함께 완성된 HTML을 보내는 하이브리드 방식입니다.
</Aside>

---

## 코드 속 시각화 3종 해부

코드에서 시각화를 담당하는 부분은 세 가지입니다. 바이브 코딩으로 업그레이드할 타깃이기도 합니다.

<div class="code-map">
  <div class="map-row">
    <div class="map-num n1">①</div>
    <div class="map-content">
      <div class="map-title">CSS 게이지 (JS 불필요)</div>
      <div class="map-desc">서버가 바늘 각도를 계산, CSS transform으로 자동 표시</div>
    </div>
  </div>
  <div class="map-row">
    <div class="map-num n2">②</div>
    <div class="map-content">
      <div class="map-title"><code>drawChart(id, data)</code></div>
      <div class="map-desc">Canvas 꺾은선 + 평균선 — RSSI 이력 시각화</div>
    </div>
  </div>
  <div class="map-row">
    <div class="map-num n3">③</div>
    <div class="map-content">
      <div class="map-title"><code>drawChannelChart(id, ch)</code></div>
      <div class="map-desc">채널별 AP 수 색상 막대차트 — 혼잡도 파악</div>
    </div>
  </div>
</div>

<Aside type="tip" title="바이브 코딩 타깃">
AI에게 요청할 때, **전체 코드 대신 해당 함수만 붙여넣어** 수정을 요청하면 훨씬 정확한 결과를 받을 수 있습니다.
</Aside>

---

## 바이브 코딩 미션

기본 시각화 3종이 정상 작동하는 것을 확인했으면, 아래 미션 카드를 순서대로 도전해 보세요.

### 미션 카드

<div class="mission-card">
  <div class="mission-header">
    <span class="mission-title">미션 1: 게이지 업그레이드</span>
    <span class="mission-difficulty">난이도 ★★☆☆☆</span>
  </div>
  <div class="mission-body">
    <div class="prompt-box prompt-full">
      <p>"게이지를 더 크게 만들고, 배경에 눈금을 추가해줘. 신호 강도에 따라 바늘 색이 초록 → 노랑 → 빨강으로 변하게."</p>
      <p>[게이지 관련 HTML+CSS+JS 붙여넣기]</p>
    </div>
    <div class="prompt-box prompt-light">
      <p>"게이지 더 크고 눈금 있게 바꿔줘"</p>
      <p>[게이지 관련 코드 붙여넣기]</p>
    </div>
  </div>
</div>

<div class="mission-card">
  <div class="mission-header">
    <span class="mission-title">미션 2: 그래프에 통계 추가</span>
    <span class="mission-difficulty">난이도 ★★★☆☆</span>
  </div>
  <div class="mission-body">
    <div class="prompt-box prompt-full">
      <p>"RSSI 이력 그래프에 최대값, 최소값, 평균값을 점선으로 표시하고 수치도 라벨로. 평균선은 노랑, 최대/최소는 회색 점선."</p>
      <p>[drawChart 함수 붙여넣기]</p>
    </div>
    <div class="prompt-box prompt-light">
      <p>"그래프에 최대/최소/평균 선 추가"</p>
      <p>[drawChart 함수 붙여넣기]</p>
    </div>
  </div>
</div>

<div class="mission-card">
  <div class="mission-header">
    <span class="mission-title">미션 3: 채널 추천 기능</span>
    <span class="mission-difficulty">난이도 ★★★☆☆</span>
  </div>
  <div class="mission-body">
    <div class="prompt-box prompt-full">
      <p>"채널 분포 차트 아래에 '추천 채널' 표시. AP가 가장 적은 채널을 찾아서 '채널 ○번이 가장 한가합니다' 메시지."</p>
      <p>[drawChannelChart 함수 붙여넣기]</p>
    </div>
    <div class="prompt-box prompt-light">
      <p>"가장 한가한 채널 추천해줘"</p>
      <p>[drawChannelChart 함수 붙여넣기]</p>
    </div>
  </div>
</div>

<div class="mission-card">
  <div class="mission-header">
    <span class="mission-title">미션 4: 새 차트 추가</span>
    <span class="mission-difficulty">난이도 ★★★★☆</span>
  </div>
  <div class="mission-body">
    <div class="mission-desc">아이디어 중 골라서 도전!</div>
    <div class="prompt-box prompt-light">
      <p>"메모리 사용량을 도넛 차트로 보여줘"</p>
      <p>"AP별 신호 강도 수평 막대 그래프 추가해줘"</p>
      <p>"신호 품질을 5칸 배터리 아이콘으로 표시해줘"</p>
      <p>"RSSI 변화량을 위아래 화살표로 표시해줘"</p>
      <p>[make_page() 붙여넣기]</p>
    </div>
  </div>
</div>

<div class="mission-card">
  <div class="mission-header">
    <span class="mission-title">보너스: 시각화 자유 설계</span>
    <span class="mission-difficulty">난이도 ★★★★★</span>
  </div>
  <div class="mission-body">
    <div class="prompt-box prompt-light">
      <p>"AP 신호를 레이더/거미줄 차트로 그려줘"</p>
      <p>"전체 테마를 레트로 게임 스타일로 바꿔줘"</p>
      <p>"RSSI를 음파 시각화 느낌으로 애니메이션 해줘"</p>
      <p>"히트맵 스타일로 시간대별 RSSI 보여줘"</p>
    </div>
  </div>
</div>

---

## 친구 대시보드 구경

미션을 진행하다 보면 친구들의 대시보드도 궁금해질 거예요. 옆 친구의 **Pico IP 주소**를 브라우저에 입력하면 친구의 대시보드를 바로 볼 수 있습니다.

어떤 차트를 추가했는지, 어떤 색상을 사용했는지 구경하고 영감을 얻어 보세요. 같은 코드에서 시작했지만, 바이브 코딩 미션을 어떻게 수행했느냐에 따라 완전히 다른 대시보드가 됩니다.

<Aside type="tip" title="그래프가 안 나올 때">
페이지를 열고 **30초 정도 기다리세요.** 데이터가 2개 이상 쌓여야 꺾은선 그래프가 그려집니다. 자동 갱신(5초)이 데이터를 계속 추가합니다.
</Aside>

---

## 핵심 용어

<div class="key-terms">
  <dl>
    <dt>Canvas API</dt>
    <dd>HTML에서 JavaScript로 그래프, 도형을 그리는 기술</dd>
    <dt>게이지</dt>
    <dd>현재 상태를 바늘이나 반원으로 표시하는 시각화</dd>
    <dt>꺾은선 그래프</dt>
    <dd>시간에 따른 변화(추세)를 보여주는 차트</dd>
    <dt>막대차트</dt>
    <dd>항목 간 크기를 비교하는 차트</dd>
    <dt>클라이언트 사이드</dt>
    <dd>서버가 아닌 브라우저에서 처리하는 방식</dd>
  </dl>
</div>

---

## 다음 시간 예고

다음 시간에는 교실을 직접 돌아다니며 위치별로 RSSI를 측정하고, **Wi-Fi 신호 지도**를 만들어 봅니다. 오늘 만든 시각화 경험이 큰 도움이 됩니다.

---

## 셀프 체크

<div class="self-check">
  <strong>오늘 수업이 끝나기 전에 확인하세요</strong>
  <ul>
    <li>3차시 코드를 Pico에 저장하고 대시보드가 정상 작동한다</li>
    <li>반원 게이지에 RSSI 바늘이 움직인다</li>
    <li>꺾은선 그래프에 데이터 포인트가 쌓인다</li>
    <li>채널 분포 막대차트가 표시된다</li>
    <li>바이브 코딩 미션을 1개 이상 완료했다</li>
    <li>Canvas API가 무엇인지 한 문장으로 설명할 수 있다</li>
  </ul>
</div>
